import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log('Adding more users...'); const branchManagerRole = await prisma.role.findUnique({ where: { name: 'Branch Manager' } }); const branch1 = await prisma.branch.findUnique({ where: { code: 'BR-001' } }); const bcrypt = require('bcryptjs'); const erode = await prisma.staff.upsert({ where: { phone: '9876543212' }, update: {}, create: { name: 'Jeena (Erode)', username: 'erode', phone: '9876543212', password: await bcrypt.hash('password123', 10), isActive: true, branchId: branch1.id, roleId: branchManagerRole.id } }); console.log('Added erode user'); } main().catch(console.error).finally(() => prisma.$disconnect());
